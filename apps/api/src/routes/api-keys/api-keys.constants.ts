/**
 * API key route constants.
 * Domain: API key management endpoints.
 */

/** Default number of API keys returned per page when not specified. */
export const DEFAULT_API_KEY_LIMIT = 25;

/** Maximum number of API keys returned per page. */
export const MAX_API_KEY_LIMIT = 100;
