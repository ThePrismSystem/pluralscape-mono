/**
 * Custom-front-route constants.
 * Domain: custom front CRUD routes and service layer.
 */

// ── Size Limits ────────────────────────────────────────────────────

/** Maximum size of encrypted custom front data in bytes (64 KiB). */
export const MAX_ENCRYPTED_DATA_BYTES = 65_536;

// ── Pagination ─────────────────────────────────────────────────────

/** Default page size for custom front listing. */
export const DEFAULT_CUSTOM_FRONT_LIMIT = 25;

/** Maximum page size for custom front listing. */
export const MAX_CUSTOM_FRONT_LIMIT = 100;
