/**
 * Hex encoding constants.
 * Domain: shared hex byte-to-string conversion.
 */

/** Radix for hexadecimal encoding (base 16). */
export const HEX_RADIX = 16;

/** Width of a single byte in hex characters (zero-padded). */
export const HEX_BYTE_WIDTH = 2;
