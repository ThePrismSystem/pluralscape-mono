/** Maximum number of active (non-archived) friend codes per account. */
export const MAX_FRIEND_CODES_PER_ACCOUNT = 10;

/** Number of random bytes used to generate the friend code string (yields 8 base36 chars). */
export const FRIEND_CODE_BYTES = 5;
