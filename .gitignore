# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/

# Turbo
.turbo/

# Expo
.expo/

# Bun
bun.lockb

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment and secrets
.env
.env.*
!.env.example
!.env.sp-test.example
!.env.pk-test.example
*.pem
*.key

# CodeQL
.codeql-db/
.codeql-results/

# Databases
*.db
*.db-shm
*.db-wal
*.db-journal

# Testing
coverage/
.nyc_output/
test-results/
playwright-report/

# Private planning docs (not for public repo)
docs/planning/project-scope.md

# Local audits (not committed unless explicitly requested)
docs/local-audits/

# Superpowers specs and plans (local working docs)
docs/superpowers/

# Git worktrees (local workspaces)
.worktrees/

# Superpowers (per-contributor)
.superpowers/

# Agent config (per-contributor)
CLAUDE.md
.claude/

# Security audit working files
security-audit-results.tsv

# UI design explorations (local/scratch work)
ui-design-local/

# Temporary files
.tmp/
*.log
*.patch
.pnpm-debug.log

# SP import test artifacts (seeded account credentials + manifests)
.env.sp-test
.env.sp-test-*
scripts/.sp-test-*.json

# PK import test artifacts (seeded account manifests, export fixtures, env)
scripts/.pk-seed-*.json
scripts/.pk-test-*.json
# Google Cloud service account keys (belt-and-suspenders; keys should live outside repo)
pluralscape-*.json
.env.pk-test

# Crowdin setup script JSON output (local artifact when running crowdin:setup --json > file)
crowdin-setup-output.json

# OS
.tmp
